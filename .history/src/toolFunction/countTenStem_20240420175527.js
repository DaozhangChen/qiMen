const baseTenStemInfo = [
    { upStem: "甲", downStem: "甲", content: "" }
]
function countTenStem(curInfo, curArea) {
    const targetInfo = curInfo.find((item) => item.number === curArea)

}
export default countTenStem